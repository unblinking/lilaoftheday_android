<?xml version="1.0" encoding="utf-8"?>

<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lilaoftheday.lilaoftheday"
    >

    <!-- Include required permissions to reschedule alarm on reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        >

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Register the Alarm Receiver -->
        <receiver android:name=".AlarmReceiver" />
        <!-- Register the Alarm Boot Receiver, for rescheduling the alarm after a device reboot -->
        <receiver android:name=".AlarmBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".PreferencesActivity"
            android:label="Preferences"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.lilaoftheday.lilaoftheday.MainActivity" />
        </activity>

        <activity
            android:name=".PhotoActivity"
            android:label="Photo"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.lilaoftheday.lilaoftheday.MainActivity" />
        </activity>

    </application>

</manifest>

