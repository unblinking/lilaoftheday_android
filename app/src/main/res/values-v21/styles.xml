<resources>

    <!--
          Base application theme for API 21+. This theme replaces
          AppTheme from resources/values/styles.xml on API 21+ devices.
      -->
    <style name="AppTheme" parent="AppTheme.Base">
        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowAllowEnterTransitionOverlap">true</item>
        <item name="android:windowAllowReturnTransitionOverlap">true</item>
        <item name="android:windowSharedElementEnterTransition">@android:transition/move</item>
        <item name="android:windowSharedElementExitTransition">@android:transition/move</item>
        <item name="android:windowBackground">@color/WindowBackground</item>
        <!-- This can set a transparent status bar, but requires too much additional work -->
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:navigationBarColor">@color/TextColorPrimary</item>
        <item name="android:textColorPrimary">@color/TextColorPrimary</item>
        <item name="preferenceTheme">@style/LilaPreferenceTheme</item>
    </style>

    <style name="Toolbar" parent="Theme.AppCompat.Light">
        <item name="android:textColorPrimary">@color/TextColorPrimary</item>
        <item name="android:textColorSecondary">@color/TextColorSecondary</item>
        <item name="actionMenuTextColor">@color/TextColorPrimary</item>
        <item name="colorAccent">@color/ColorAccent</item>
    </style>

    <style name="LilaPreferenceTheme" parent="@style/PreferenceThemeOverlay.v14.Material">
        <item name="android:background">@android:color/white</item>

    </style>

</resources>

