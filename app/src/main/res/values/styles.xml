<resources>

    <style name="AppTheme" parent="AppTheme.Base">
    </style>

    <!-- Base theme applied no matter what API -->
    <style name="AppTheme.Base" parent="Theme.AppCompat.Light">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="colorPrimary">@color/ColorPrimary</item>
        <item name="colorPrimaryDark">@color/ColorPrimaryDark</item>
        <item name="colorAccent">@color/ColorAccent</item>
        <item name="displayOptions">disableHome</item>
        <item name="preferenceTheme">@style/LilaPreferenceTheme</item>
    </style>

    <style name="Toolbar" parent="Theme.AppCompat.Light">
        <item name="android:textColorPrimary">@color/TextColorPrimary</item>
        <item name="android:textColorSecondary">@color/TextColorSecondary</item>
        <item name="actionMenuTextColor">@color/TextColorPrimary</item>
        <item name="colorAccent">@color/ColorAccent</item>
    </style>

    <style name="LilaPreferenceTheme" parent="@style/PreferenceThemeOverlay.v14.Material">
        <item name="android:background">@android:color/white</item>
    </style>

</resources>

